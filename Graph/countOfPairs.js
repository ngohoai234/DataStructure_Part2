/**
 * @param {number} n
 * @param {number} x
 * @param {number} y
 * @return {number[]}
 */
var countOfPairs = function (n, x, y) {};

// 1 to n connected by n streets

// 0 -> 0
// => m[0][0] + m[0][1]

// d[i][k] + d[k][j]

// i = 0, k = 0, j = 1
// 0 -> 1 => [0][0]

// i = 3, k = 0, j = 1
// 0 -> 2 => [0][0] + [0][2]
